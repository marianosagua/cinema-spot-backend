<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Restablecer contraseña</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f7f7f7; }
    .container { max-width: 400px; margin: 40px auto; background: #fff; padding: 2em; border-radius: 8px; box-shadow: 0 2px 8px #0001; }
    h2 { text-align: center; }
    form { display: flex; flex-direction: column; gap: 1em; }
    input[type="password"] { padding: 0.5em; font-size: 1em; }
    button { padding: 0.7em; background: #007bff; color: #fff; border: none; border-radius: 4px; font-size: 1em; cursor: pointer; }
    button:hover { background: #0056b3; }
  </style>
</head>
<body>
  <div class="container">
    <h2>Restablecer contraseña de CinemaSpot</h2>
    <form method="POST" action="/api/auth/restablecer-password/{{token}}">
      <input type="password" name="password" placeholder="Nueva contraseña para CinemaSpot" required minlength="6" />
      <button type="submit">Restablecer contraseña</button>
    </form>
  </div>
</body>
</html>
